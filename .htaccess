<IfModule mod_rewrite.c>
    RewriteEngine On

    # Jika folder proyek diakses langsung (mis. htdocs/Sakuci-framework),
    # arahkan semuanya ke folder public.
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>


